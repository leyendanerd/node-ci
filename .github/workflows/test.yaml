name: node test CI and deploy 

on: 
  push:
    branches:
      - master
  
  pull_request:
     branches:
      - master  
      
jobs:
  build_test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - run: |
           apt install nginx
           curl http://localhost
    





        

